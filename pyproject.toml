[project]
name = "canbuslab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "psycopg>=3.3.4",
    "python-can[serial]>=4.6.1",
]
